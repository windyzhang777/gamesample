import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  button: {
    width: 50,
    height: 50,
    borderWidth: 0,
    padding: 5,
    margin: 15,
    borderRadius: 5,
  },

  image: {
    width: 40,
    height: 40,
  },

  active: {
    borderColor: '#FFF',
    borderWidth: 2,
  },
});

export default styles;
