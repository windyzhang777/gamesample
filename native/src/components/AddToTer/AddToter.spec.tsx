import React from 'react';
import {shallow} from 'enzyme';
// import AddToter from './index';

describe('<AddToter/>', () => {
  it('should remove this passing test', () => {
    expect(true).toBeTruthy();
  });
  // it('renders wrapper', () => {
  //   expect(shallow(<AddToter />)).toMatchSnapshot();
  // });
  // it('renders component', () => {
  //   const RenderPropComponent = shallow(<AddToter />).prop('renderComponent');
  //   expect(
  //     shallow(
  //       <RenderPropComponent
  //         changeToterNameCallback={jest
  //           .fn()
  //           .mockName('A function to be called when the user types a keystroke')}
  //         addToterCallback={jest
  //           .fn()
  //           .mockName('function to be called when the add Toter button is tapped')}
  //         username={'gmom-35'}
  //       />,
  //     ),
  //   ).toMatchSnapshot();
  // });
});
