import React from 'react';
// import {shallow} from 'enzyme';
// import ManageTrickOrTreaters from './index';

describe('<ManageTrickOrTreaters/>', () => {
  it('should remove this passing test', () => {
    expect(true).toBeTruthy();
  });
  // it('renders wrapper', () => {
  //   expect(shallow(<ManageTrickOrTreaters />)).toMatchSnapshot();
  // });
  // it('renders component with a display name', () => {
  //   const RenderPropComponent = shallow(<ManageTrickOrTreaters />).prop('renderComponent');
  //   expect(
  //     shallow(<RenderPropComponent toterList={'List of Toters from Redux'} />),
  //   ).toMatchSnapshot();
  // });
});
