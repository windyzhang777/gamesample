import {StyleSheet} from 'react-native';
import {navigationSectionUnderlineColor} from 'shared/theme/variables';

export default StyleSheet.create({
  NavigationSectionUnderline: {
    height: 4,
    backgroundColor: navigationSectionUnderlineColor,
  },
});
