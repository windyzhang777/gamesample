import React, {useEffect} from 'react';
import {View} from 'react-native';

import RedeemCredits from 'src/components/RedeemCredits';

//this should be your connected file that has state, redux, nav, hooks, etc in it

function RedeemCandyCredits() {
  return <RedeemCredits />;
}

export default RedeemCandyCredits;
